# 给 Claude 的工作约定

## 协作方式

- **直接改 main，不要开 PR。** 这个仓库只有一个人在操作，开 PR 只是多一步手动合并。
  改完、测完，直接 `git push origin main`。不要建 `claude/xxx` 分支，不要问"要不要合并"。
- 推之前**必须**本地跑一遍 `python3 test_engine.py`，全过才推。直接推 main 意味着没有
  预合并的 CI 关卡了，这套用例是唯一的闸门（`.github/workflows/test.yml` 只能事后报警）。
- 改动会改变历史信号的归类时（红点/蓝点/空心/黑框的标记位置），在回复里给出前后对比：
  各类信号的天数、触发后收益、为负比例。只说"改好了"不算交付。

## 构建产物：不要手工改

`data.json` 与 `dashboard.html` 是产物，**不要手工编辑、也不要手工提交**。
推到 main 之后 `.github/workflows/daily.yml` 的 push 触发会用缓存的完整 `raw/` 重算、
重建并发布到 Pages；本地双击「更新并打开.command」同样会重新生成。
只改 `engine.py` / `dash_tpl.html` 等源文件。

## 标定的铁律：只用 raw/ 里的完整原始序列

任何阈值、参数、口径的标定，都必须跑在 **`raw/` 下的完整原始序列**上，
**不能**用 `data.json` 里的 `series.*`。后者已经截断到展示窗口（2017-10 起）、
并且对齐到纳指交易日，跟生产的口径不是一回事：

- 滚动分位算出来的数不同（比较窗口里的样本集不一样）；
- 需要预热的指标在展示窗口开头是 NaN，**那段历史在分析里完全看不见**。

2026-09 因为这条栽过一次：空心蓝点闸门的标定做在截断序列上，2018 年整段藏在分位预热期
后面，上线后才发现它把 2018-12-21/24 与 2026-03-27 三个最好的蓝点判成了空心
（后 63 日分别 +21.9% / +24.2% / +28.9%）。见 `engine.py` 里 `repricing_regime` 上方的注释。

`raw/` 不进 git（43MB 日更，会撑爆历史）：本机有，云端在 `actions/cache` 里。
容器里没有时，找用户要具体的那几个文件，不要拿 `data.json` 凑合。
