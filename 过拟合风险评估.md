# 过拟合风险评估记录

这份记录只写**做过的检验和测出来的数**，不写感想。每条结论后面都跟着复现它的命令。

最后更新：2026-09-17

---

## 零、一句话现状

样本内所有标定都出自 2017-10 至今这一段；把参数冻结后跑在更早的历史上，**2007-2010
那段的检验已被证明无效**（幸存者偏差量级 30.9pp，见第二节），**2011-2017 是目前唯一
可用的样本外窗口**。在这个窗口里：蓝点保住了边际，黑框缩水到约四分之一，红点没有
区分度，熊市反弹已因样本前符号翻转而停用。

---

## 一、方法与工具

| 脚本 | 作用 |
|---|---|
| `build_long_panel.py` | 把 stooq 长历史拼到 stockanalysis 生产序列前面 → `raw_long/` |
| `oos_check.py --raw raw_long` | 冻结当前参数，分三段报信号表现 |
| `make_oos_page.py --raw raw_long` | 以 2017-09-30 为"当日"重算，出 `dashboard_oos.html` |

**为什么要拼而不是整盘换源**：stockanalysis（主源）range 封顶 10Y，最早只到 2016-09，
给不了 2006-2017；但所有标定与线上 `data.json` 都出自它的口径。整盘换成 stooq 会在补
历史的同时静悄悄改掉近十年的读数。所以 2016-09 之后**逐字节照抄**，之前用 stooq 并按
接缝当天的比例缩放。

**拼接质量**（2026-09-17 实测 531 只）：接缝缩放误差中位 `0.00e+00`、90 分位 `1.11e-16`、
最大 `2.22e-16`，误差 > 0.5% 的 **0 只**。仅 SOXS 一只因接缝附近样本不足而失败，
`raw_long/` 里没有它，杠杆温度是用 14 只而非 15 只算的。

**自校验通过**：样本内（2017-10 起）五个信号的天数与线上 `data.json` 完全一致——
`hot 66 / hot_bear 21 / cold 29 / cold_soft 13 / crowd 43`，日级收益也逐个对上 README。
这同时验证了 `oos_check.py` 里手抄的那份接线没有走样。

---

## 二、幸存者偏差：2007-2010 的检验无效（2026-09-17）

**这是本轮最重要的发现，它推翻了同日早些时候关于红点的结论。**

`engine.SECTORS` 是**今天**的 503 只成分股。拿它回溯到 2008，等于把当年破产 / 被并购 /
跌出指数的公司全部剔除——而那批公司正是 2007-2008 宽度崩坏的主要贡献者。

用 **RSP**（等权标普500 ETF，2003 年上市，其历史持仓包含那批票，**无幸存者偏差**）
对照本地 503 只的等权组合：

| 区间 | 幸存者篮子等权 | RSP（真实等权） | 偏差 |
|---|---|---|---|
| 2007-01 ~ 2007-10（冲顶） | **+17.46%** | +6.83% | **+10.63pp** |
| 2007-10 ~ 2009-03（顶到底） | −52.08% | −60.02% | +7.95pp |
| 2007-01 ~ 2009-12（全程） | +14.48% | **−16.46%** | **+30.94pp** |

**为什么这恰好打在红点的主项上**：红点温度里权重最大的是上涨拥挤度（3/9），
它 = `(cw/cw.shift(63)) / (eq/eq.shift(63)) - 1` 与宽度变化 `brd_chg` 的合成。
`eq` 被抬高 10.6pp，比值就被压低；宽度崩坏同样被掩盖。三个分量里两个被系统性压低。

**两个温度的偏差方向相反**（早先的记录把它们混为一谈，是错的）：

| | 主要成分 | 幸存者偏差 → | 后果 |
|---|---|---|---|
| 蓝点温度 | 六项等权，**宽度越好越热** | 读数**偏高** | 蓝点偏**少** |
| 红点温度 | 上涨拥挤度 3/9，**宽度越差越高** | 读数**偏低** | 红点偏**少** |

所以 **2007-2010 那段对所有信号的检验都不成立**，不只是红点。那段的数字保留在下面
仅作记录，不作为判据。

**修不了**：需要退市公司的行情数据，而 stooq 的 `d_us_txt.zip` 只含当前在市代码——
实测 `lehmq / bsc / wamuq / gmgmq / ccrtq / abkfq` 一个都没有。历史成分股名单可以从
Wikipedia 的 changes 表重建，但没有那些公司的日线，重建了也没用。要修得换带退市数据
的源（CRSP 之类，基本都收费）。

复现：
```bash
python3 - <<'PY'
import zipfile, io, pandas as pd, engine as E
z = zipfile.ZipFile("stooq_bulk/d_us_txt.zip")
n = [x for x in z.namelist() if x.lower().endswith("/rsp.us.txt")]
d = pd.read_csv(io.BytesIO(z.read(n[0])))
dc = next(c for c in d.columns if "DATE" in c.upper())
cc = next(c for c in d.columns if "CLOSE" in c.upper())
rsp = pd.Series(pd.to_numeric(d[cc],errors="coerce").values,
                index=pd.to_datetime(d[dc].astype(str),format="%Y%m%d",errors="coerce")).dropna().sort_index()
E.RAW = "raw_long"
px = pd.DataFrame({t: v["close"] for t, v in E.load_many(E.STOCKS).items()})
eq = (1 + px.pct_change().mean(axis=1)).cumprod()
for a, b in (("2007-01-01","2007-10-31"),("2007-10-09","2009-03-09"),("2007-01-01","2009-12-31")):
    i = eq.index[(eq.index>=a)&(eq.index<=b)]; j = rsp.index[(rsp.index>=a)&(rsp.index<=b)]
    e = eq[i[-1]]/eq[i[0]]-1; r = rsp[j[-1]]/rsp[j[0]]-1
    print(f"{a}~{b}  幸存者 {e*100:+7.2f}%  RSP {r*100:+7.2f}%  偏差 {(e-r)*100:+6.2f}pp")
PY
```

---

## 三、样本外表现（2026-09-17，拼接修好后的干净数据）

前瞻 63 交易日 QQQ。「事件」= 63 日窗口不重叠的独立触发段。

### 样本前 A：2006-2010（**已证无效，仅存档**）　基准 +2.23% / 35%为负

| 信号 | 天/段/事件 | 日级 | 为负 | 事件级 | 最差事件 |
|---|---|---|---|---|---|
| hot | 0 | — | — | — | — |
| hot_bear | 42/9/2 | +9.50% | 10% | −13.03% | −30.6% |
| cold | 37/16/2 | +1.99% | 49% | −4.62% | −10.0% |
| cold_soft | 0 | — | — | — | — |
| crowd | 0 | — | — | — | — |

### 样本前 B：2011-2017（**目前唯一可用的样本外窗口**）　基准 +4.20% / 21%为负

| 信号 | 天/段/事件 | 日级 | 为负 | 事件级 | 边际（事件级−基准） |
|---|---|---|---|---|---|
| hot | 6/2/2 | +4.59% | 0% | +7.62% | **+3.4pp（反向，无区分度）** |
| hot_bear | 0 | — | — | — | 未触发 |
| cold | 23/12/2 | +9.82% | 0% | +10.91% | **+6.7pp（保住）** |
| cold_soft | 0 | — | — | — | 未触发 |
| crowd | 9/4/3 | +3.19% | 11% | +2.24% | **−2.0pp（方向对，缩水）** |

### 样本内：2017-10 至今（标定窗口，对照用）　基准 +5.17% / 27%为负

| 信号 | 天/段/事件 | 日级 | 为负 | 事件级 | 边际 |
|---|---|---|---|---|---|
| hot | 66/23/8 | −3.34% | 70% | −2.83% | −8.0pp |
| hot_bear | 21/6/1 | −10.04% | 81% | −4.43% | −9.6pp |
| cold | 29/16/6 | +27.49% | 0% | +17.40% | +12.2pp |
| cold_soft | 13/5/1 | −8.69% | 100% | −7.99% | −13.2pp |
| crowd | 43/16/8 | −2.01% | 67% | −2.57% | −7.7pp |

---

## 四、逐信号结论

### 熊市反弹（空心红点）— **已于 2026-09-17 停用**

样本内最好看的一条（日均 −10.04% / 81%为负），样本前整个翻过来：2006-2010 触发
42 天 / 9 段，日级 **+9.50%、只有 10% 为负**。触发区间 2008-07-23 ~ 2009-05-15，
而 2009-03-09 是那轮大底——叫对了开头，然后在底部和底部之后一直在叫减仓。

决定性的是证据结构：全样本只有两轮熊市，一轮对一轮错，而**对的那轮正是它被造出来
要覆盖的那轮**（`BEAR_TH=60` 就是为了让 2022 触发才定的）。零样本外支持。

停用而非降级，是因为判错的代价是在大底减仓。完整记录见 `engine.py` 的 `BEAR_TH` 上方，
常量保留可回退。

> 注：这一段的结论建立在 2006-2010 的数据上，而第二节已证明那段有严重幸存者偏差。
> 但偏差的方向是**让宽度读数偏好、温度偏低**，即让这条信号**少触发**；它却触发了
> 42 天并且方向是反的，所以偏差不能解释这个结果，结论不受影响。

### 红点 — 样本外**未经有效检验**，不是"检验不通过"

- 2007-2010：一天都没触发。最高温 74.3（@ 2009-04-15，反弹期而非顶部），
  `>75` 的天数为 **0**。但第二节已证明这段无效——真实的 2007-10 是"指数创新高 +
  一大批股票在崩"，正是红点该抓的形态，而数据里崩的那部分被删掉了。
- 2011-2017：触发 6 天 / 2 段 / 2 事件，事件级 +7.62%，**高于基准 +4.20%**，没有区分度。
  样本内是 −8.0pp，样本外是 +3.4pp。
- 结论：红点目前**没有任何一个可用的样本外窗口给出正面证据**。2011-2017 那 2 个事件
  是反向的，但 n=2。别按样本内的 −3.34% 设仓位。

### 蓝点 — 唯一在样本外保住边际的信号

2011-2017 事件级 +10.91% / 0%为负，边际 +6.7pp；样本内 +12.2pp。只缩了 5.5pp。
骨架简单（两个参数、是交集不是加权）、`VIX ≥ 30` 是外生绝对刻度而非从数据里标出来的，
这些大概是它能迁移的原因。

### 黑框 — 方向对，边际缩到约四分之一

2011-2017 边际 −2.0pp，样本内 −7.7pp。降级为参考，不作为减仓依据。

### 空心蓝点 — 仍是零样本外验证

两段样本外窗口内都**一次未触发**（实际利率重估期在 2007-2017 从未成立）。
它判别的宏观状态在全部可得历史里只出现过一次（2021H2–2022），维持"只降级不屏蔽"。

---

## 五、方法论上的两条硬约束

### 1. 自由度远多于独立事件

约 40 个活参数，对着的独立事件（63 日窗口不重叠）是：

| | 段数 | **独立事件** |
|---|---|---|
| 红点 | 23 | 8 |
| 黑框 | 16 | 8 |
| 蓝点 | 16 | 6 |
| 熊市反弹 | 6 | 1 |
| 空心蓝点 | 5 | 1 |

而且三条减仓信号不是三份独立证据：红点与黑框有 22 个触发日完全同日，合并后整个减仓侧
只剩 **10 个独立事件**。注释里"23 段 / 66 天"给人的样本感是虚高的，日级均值的标准误
按天算会严重低估。

### 2. 「两段子样本同号」是筛选条件，不是验证

三处都把后段用作了**选择函数**：

- `SELL_W`：16384 种权重组合先筛"两段秩相关同号"留下 15575 个，再按顶部十分位收益排序
- `CROWD_NARROW` 取 75 不取 70：理由原话是"正因为 70 的**后段会翻正**"
- `LEV_W` 3:1：扫描表里前段/后段是并列两列，一起看着挑的

一旦后段进了选择函数，"后段同号"就不再是验证证据。推论：即使在用后段挑过参数之后，
红点后段的边际仍只剩 4.3pp（触发后 +0.38% vs 基准 +4.68%），真样本外只会更低。

---

## 六、未决

- **2011-2017 的幸存者偏差未量化。** 该段退市公司以被并购为主而非破产，偏差应远小于
  2007-2010，但没量过。量法同第二节，把区间换成 2011-2017，并拿样本内（2017-10 起）
  的年化偏差当零点对照。在这之前，第三节里 2011-2017 的数字要带保留地读。
- **`SOXS` 缺失**，杠杆温度用 14 只而非 15 只算。
- **真样本外记录仍接近零**。当前参数定于 2026-09-10 之后；按每年 1–2 次触发的频率，
  要攒出有意义的前瞻证据需要 2–3 年，且每改一次参数这个计数就清零。
